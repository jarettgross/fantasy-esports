extends layout

block scripts
	script(type='text/javascript').
		var contestInfo = JSON.parse("#{contest}".replace(/&quot;/g,'"'));
		var contestUsers = JSON.parse("#{contestUsers}".replace(/&quot;/g,'"'));

block content
	include partials/header
	.section-wrapper#contest-wrapper